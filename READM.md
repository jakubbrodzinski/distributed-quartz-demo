# Distributed Quartz Jobs with Postgres & SpringBoot
## Deploy:
```
chmod a+x mvnw
chmod a+x *.sh
./build.sh
./deploy.sh
docker service logs quartz_app
```

## Requirements:

**Docker should be in swarm mode**